<div class="article-title">
	<h1 class="page-title">Projects</h1>
</div>


<div class="container margin-sm">
	<% @projects.each do |project| %>
		<%= link_to project, :class=>'m-item-index' do %>
			<h4 class="u-title">
				<%= project.title %>
			</h4>			
			<h5 class="u-date">Show More</h5>
		<% end %>
	<% end %>
</div>